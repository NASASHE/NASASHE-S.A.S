rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Regla de transición: NO rompe la versión actual
      allow read, write: if request.auth != null
                         || request.time < timestamp.date(2026, 10, 5);
    }
  }
}